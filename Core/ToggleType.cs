using UnityEngine;

namespace net.narazaka.avatarmenucreator
{
    public enum ToggleType
    {
        [InspectorName("制御しない")]
        None,
        [InspectorName("ON")]
        ON,
        [InspectorName("OFF")]
        OFF,
    }
}
